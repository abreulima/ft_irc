#include "../includes/Server.hpp"

int main()
{
    Server server;

    /* while 
     *  {
     *      
     *      if (MensagemRecebida)
     *      {
     *          
     *          Client* client = null;
     * 
     *          if (NovaConexao?)
     *              client = Server.Add(MensagemRecebida)
     *          else
     *              client = Server.GetClient(MensagemRecebida)
     *      
     *          Command command = Command(client.Message)
     *          {
     *              if (command.Process() == CommandType::JOIN)
     *              {
     *                  Server.JoinChannel(command.join.channel, client);
     *              }
     *              
     *              else if (command.Process() == CommandType::NICKL)
     *              {
     *                  Client.ChangeNick(command.Nick.nick)
     *              }
     * 
     *              ....
     * 
     *          }
     * 
     *          // agora processa nova mensagem
     *          
     * 
     *      }
     *  
     *  }
     * 
     * 
     */
    
}

